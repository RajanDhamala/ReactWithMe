Throgh this password genrating react project user can generate random password with choice to whether
include characters and numbers inside the password and length can also be set by the user.
learned a lot about diff types of hooks and their uses
